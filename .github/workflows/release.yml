# Release the npm package and create the matching GitHub release.
# yaml-language-server: $schema=https://www.schemastore.org/github-workflow.json
name: "Release"

run-name: >-
    ${{
        github.event_name == 'workflow_dispatch' &&
        format(
            'Manual release ({0}) by @{1}',
            inputs.version || inputs.release_type,
            github.actor
        ) ||
        format('Tagged release {0} by @{1}', github.ref_name, github.actor)
    }}

concurrency:
    group: "${{ github.workflow }}-${{ github.repository }}"
    cancel-in-progress: "${{ !startsWith(github.ref, 'refs/tags/v') }}"

on:
    push:
        tags:
            - "v*"
    workflow_dispatch:
        inputs:
            release_type:
                description: "Release bump type"
                required: true
                default: "patch"
                type: "choice"
                options:
                    - "patch"
                    - "minor"
                    - "major"
            version:
                description: "Explicit release version in x.y.z format (overrides release_type)"
                required: false
                type: "string"
defaults:
    run:
        shell: "bash"

jobs:
    publish:
        name: "Publish npm package and GitHub release"
        if: "${{ github.event_name != 'push' || github.actor != 'github-actions[bot]' }}"
        runs-on: "ubuntu-latest"
        timeout-minutes: 60
        outputs:
            package_name: "${{ steps.version.outputs.package_name }}"
            version: "${{ steps.version.outputs.version }}"
            tag: "${{ steps.version.outputs.tag }}"
            npm_release_url: "${{ steps.version.outputs.npm_release_url }}"
            github_release_url: "${{ steps.version.outputs.github_release_url }}"
        permissions:
            contents: "write"
            id-token: "write"
        env:
            CI: "true"
            DEFAULT_RELEASE_BRANCH: "main"
            NPM_REGISTRY_URL: "https://registry.npmjs.org"
        concurrency:
            group: "release-pipeline"
            cancel-in-progress: false

        steps:
            - name: "Harden the runner (Audit all outbound calls)"
              uses: "step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411" # v2.19.4
              with:
                  egress-policy: "audit"

            - name: "Resolve target ref"
              id: "target_ref"
              run: |
                  set -euo pipefail
                  if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
                    echo "ref=${DEFAULT_RELEASE_BRANCH}" >> "$GITHUB_OUTPUT"
                    echo "branch=${DEFAULT_RELEASE_BRANCH}" >> "$GITHUB_OUTPUT"
                  else
                    echo "ref=${GITHUB_REF}" >> "$GITHUB_OUTPUT"
                    echo "branch=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
                  fi

            - name: "Checkout repository"
              uses: "actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0" # v7.0.0
              with:
                  fetch-depth: 0
                  fetch-tags: true
                  ref: "${{ steps.target_ref.outputs.ref }}"
                  token: "${{ secrets.GITHUB_TOKEN }}"

            - name: "Setup Node.js"
              uses: "actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e" # v6.4.0
              with:
                  node-version-file: ".node-version"
                  package-manager-cache: false
                  registry-url: "${{ env.NPM_REGISTRY_URL }}"

            - name: "Setup repository npm version"
              run: |
                  set -euo pipefail
                  package_manager=$(node -p "require('./package.json').packageManager")
                  if [[ ! "$package_manager" =~ ^npm@([0-9]+\.[0-9]+\.[0-9]+)$ ]]; then
                    echo "packageManager must pin an exact npm version, received: $package_manager" >&2
                    exit 1
                  fi
                  expected_npm_version="${BASH_REMATCH[1]}"
                  npm install --global "npm@${expected_npm_version}" --ignore-scripts
                  actual_npm_version=$(npm --version)
                  if [ "$actual_npm_version" != "$expected_npm_version" ]; then
                    echo "Expected npm ${expected_npm_version}, received ${actual_npm_version}." >&2
                    exit 1
                  fi

            - name: "Install native lint tools"
              run: |
                  set -euo pipefail
                  go install github.com/zricethezav/gitleaks/v8@83d9cd684c87d95d656c1458ef04895a7f1cbd8e # v8.30.1
                  echo "${HOME}/go/bin" >> "${GITHUB_PATH}"
                  python -m pip install --user --disable-pip-version-check yamllint==1.38.0
                  echo "${HOME}/.local/bin" >> "${GITHUB_PATH}"

            - name: "Install dependencies"
              run: "npm ci"

            - name: "Configure Git for release automation"
              if: "github.event_name == 'workflow_dispatch'"
              run: |
                  set -euo pipefail
                  git config user.name "github-actions[bot]"
                  git config user.email "github-actions[bot]@users.noreply.github.com"
                  git fetch --tags --force

            - name: "Validate workflow_dispatch release input"
              if: "github.event_name == 'workflow_dispatch'"
              env:
                  EXPLICIT_VERSION: "${{ inputs.version }}"
                  RELEASE_TYPE: "${{ inputs.release_type }}"
              run: |
                  set -euo pipefail
                  explicit_version="${EXPLICIT_VERSION:-}"
                  if [ -n "$explicit_version" ]; then
                    if [[ ! "$explicit_version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
                      echo "Invalid version input '$explicit_version'. Use x.y.z format." >&2
                      exit 1
                    fi
                    exit 0
                  fi

                  release_type="${RELEASE_TYPE:-patch}"
                  case "$release_type" in
                    patch|minor|major)
                      exit 0
                      ;;
                    *)
                      echo "Invalid release_type '$release_type'. Allowed: patch, minor, major." >&2
                      exit 1
                      ;;
                  esac

            - name: "Validate tag matches package version"
              if: "startsWith(github.ref, 'refs/tags/v')"
              run: |
                  TAG_VERSION="${GITHUB_REF_NAME#v}"
                  PACKAGE_VERSION=$(node -p "require('./package.json').version")
                  if [ "$TAG_VERSION" != "$PACKAGE_VERSION" ]; then
                    echo "Tag version ($TAG_VERSION) does not match package.json version ($PACKAGE_VERSION)."
                    exit 1
                  fi

            - name: "Capture pre-verification worktree state"
              run: |
                  set -euo pipefail
                  git status --porcelain=v1 --untracked-files=all > "${RUNNER_TEMP}/release-status-before.txt"

            - name: "Verify package"
              run: "npm run release:check"

            - name: "Ensure verification preserved the complete worktree state"
              run: |
                  set -euo pipefail
                  git status --porcelain=v1 --untracked-files=all > "${RUNNER_TEMP}/release-status-after.txt"
                  if ! cmp -s "${RUNNER_TEMP}/release-status-before.txt" "${RUNNER_TEMP}/release-status-after.txt"; then
                    echo "release:check changed the worktree." >&2
                    diff -u "${RUNNER_TEMP}/release-status-before.txt" "${RUNNER_TEMP}/release-status-after.txt" || true
                    git status --short
                    exit 1
                  fi

            - name: "Determine release version"
              id: "version"
              env:
                  EXPLICIT_VERSION: "${{ inputs.version }}"
                  RELEASE_TYPE: "${{ inputs.release_type }}"
              run: |
                  set -euo pipefail

                  if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
                    explicit_version="${EXPLICIT_VERSION:-}"
                    if [ -n "$explicit_version" ]; then
                      npm version "$explicit_version" --no-git-tag-version
                      version="$explicit_version"
                    else
                      release_type="${RELEASE_TYPE:-patch}"
                      npm version "$release_type" --no-git-tag-version
                      version=$(node -p "require('./package.json').version")
                    fi
                  else
                    tag="${GITHUB_REF_NAME}"
                    version="${tag#v}"
                  fi

                  package_name=$(node -p "require('./package.json').name")
                  tag="v${version}"
                  npm_release_url="https://www.npmjs.com/package/${package_name}/v/${version}"
                  github_release_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/releases/tag/${tag}"

                  {
                    echo "version=$version"
                    echo "tag=$tag"
                    echo "package_name=$package_name"
                    echo "npm_release_url=$npm_release_url"
                    echo "github_release_url=$github_release_url"
                  } >> "$GITHUB_OUTPUT"

            - name: "Rebuild package after manual version bump"
              if: "github.event_name == 'workflow_dispatch'"
              run: "npm run build"

            - name: "Refresh changelog after manual version bump"
              if: "github.event_name == 'workflow_dispatch'"
              run: "npm run changelog:generate"

            - name: "Check whether version is already published"
              id: "npm_check"
              env:
                  PACKAGE_NAME: "${{ steps.version.outputs.package_name }}"
                  VERSION: "${{ steps.version.outputs.version }}"
              run: |
                  set -euo pipefail
                  if npm view "${PACKAGE_NAME}@${VERSION}" version >/dev/null 2>&1; then
                    echo "already_published=true" >> "$GITHUB_OUTPUT"
                    echo "Version ${VERSION} is already published to npm."
                  else
                    echo "already_published=false" >> "$GITHUB_OUTPUT"
                    echo "Version ${VERSION} is available for publishing."
                  fi

            - name: "Prevent duplicate workflow_dispatch release version"
              if: "github.event_name == 'workflow_dispatch' && steps.npm_check.outputs.already_published == 'true'"
              env:
                  VERSION: "${{ steps.version.outputs.version }}"
              run: |
                  echo "Version ${VERSION} is already published. Choose a new release type/version." >&2
                  exit 1

            - name: "Prevent duplicate workflow_dispatch tag or GitHub release"
              if: "github.event_name == 'workflow_dispatch'"
              env:
                  GH_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
                  TAG: "${{ steps.version.outputs.tag }}"
              run: |
                  set -euo pipefail
                  if git ls-remote --exit-code --tags origin "refs/tags/${TAG}" >/dev/null 2>&1; then
                    echo "Tag ${TAG} already exists on origin." >&2
                    exit 1
                  fi
                  if gh release view "$TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
                    echo "GitHub release ${TAG} already exists." >&2
                    exit 1
                  fi

            - name: "Build package for publish"
              run: "npm run build:runtime"

            - name: "Create and validate release archives"
              id: "release_assets"
              env:
                  PACKAGE_NAME: "${{ steps.version.outputs.package_name }}"
                  VERSION: "${{ steps.version.outputs.version }}"
              run: |
                  set -euo pipefail

                  assets_dir="${RUNNER_TEMP}/release-assets"
                  unpack_dir="${RUNNER_TEMP}/release-package"
                  pack_metadata_path="${assets_dir}/npm-pack.json"
                  mkdir -p "$assets_dir"
                  rm -rf "$unpack_dir"

                  npm pack --json --ignore-scripts --pack-destination "$assets_dir" > "$pack_metadata_path"

                  tarball_filename=$(
                    node -e "
                      const fs = require('node:fs');
                      const packMetadata = JSON.parse(fs.readFileSync(process.argv[1], 'utf8'));
                      const records = Array.isArray(packMetadata)
                        ? packMetadata
                        : Object.values(packMetadata);
                      if (records.length !== 1) {
                        throw new Error('Expected exactly one npm pack record.');
                      }
                      const filename = records[0]?.filename;
                      if (typeof filename !== 'string' || filename.trim().length === 0) {
                        throw new Error('npm pack did not provide a valid filename.');
                      }
                      process.stdout.write(filename);
                    " "$pack_metadata_path"
                  )
                  tarball_path="${assets_dir}/${tarball_filename}"
                  safe_package_name=$(printf '%s' "$PACKAGE_NAME" | sed 's/^@//; s#/#-#g')
                  zip_basename="${safe_package_name}-${VERSION}"

                  mkdir -p "$unpack_dir"
                  tar -xzf "$tarball_path" -C "$unpack_dir"
                  node -e "
                    const fs = require('node:fs');
                    const manifest = JSON.parse(fs.readFileSync(process.argv[1], 'utf8'));
                    const expectedName = process.argv[2];
                    const expectedVersion = process.argv[3];
                    if (manifest.name !== expectedName || manifest.version !== expectedVersion) {
                      throw new Error(
                        'Packed manifest mismatch: expected ' + expectedName + '@' + expectedVersion +
                        ', received ' + manifest.name + '@' + manifest.version + '.'
                      );
                    }
                  " "$unpack_dir/package/package.json" "$PACKAGE_NAME" "$VERSION"
                  mv "$unpack_dir/package" "$unpack_dir/${zip_basename}"

                  (
                    cd "$unpack_dir"
                    zip -rq "${assets_dir}/${zip_basename}.zip" "${zip_basename}"
                  )

                  {
                    echo "tarball_path=$tarball_path"
                    echo "zip_path=${assets_dir}/${zip_basename}.zip"
                    echo "tarball_name=$tarball_filename"
                    echo "zip_name=${zip_basename}.zip"
                  } >> "$GITHUB_OUTPUT"

            - name: "Commit and push release version + tag"
              if: "github.event_name == 'workflow_dispatch'"
              env:
                  BRANCH: "${{ steps.target_ref.outputs.branch }}"
                  TAG: "${{ steps.version.outputs.tag }}"
              run: |
                  set -euo pipefail

                  if git rev-parse "$TAG" >/dev/null 2>&1; then
                    echo "Tag $TAG already exists locally." >&2
                    exit 1
                  fi

                  mapfile -t changed_paths < <(git diff --name-only)
                  mapfile -t untracked_paths < <(git ls-files --others --exclude-standard)

                  if [ "${#untracked_paths[@]}" -ne 0 ]; then
                    echo "Unexpected untracked files before the release commit:" >&2
                    printf ' - %s\n' "${untracked_paths[@]}" >&2
                    exit 1
                  fi

                  for path in "${changed_paths[@]}"; do
                    case "$path" in
                      CHANGELOG.md|package.json|package-lock.json)
                        ;;
                      *)
                        echo "Unexpected tracked release mutation: $path" >&2
                        exit 1
                        ;;
                    esac
                  done

                  git add -- CHANGELOG.md package.json package-lock.json
                  mapfile -t staged_paths < <(git diff --cached --name-only)
                  for path in "${staged_paths[@]}"; do
                    case "$path" in
                      CHANGELOG.md|package.json|package-lock.json)
                        ;;
                      *)
                        echo "Unexpected staged release path: $path" >&2
                        exit 1
                        ;;
                    esac
                  done
                  if git diff --cached --quiet; then
                    echo "No staged changes found for release commit." >&2
                    exit 1
                  fi

                  git commit -m "🔖 [chore] Release ${TAG}"
                  if ! git diff --quiet || ! git diff --cached --quiet || [ -n "$(git ls-files --others --exclude-standard)" ]; then
                    echo "Release commit left unexpected worktree changes." >&2
                    git status --short
                    exit 1
                  fi
                  git tag -a "$TAG" -m "$TAG"
                  git push origin "HEAD:${BRANCH}"
                  git push origin "$TAG"

            - name: "Generate release notes from git-cliff"
              id: "release_notes"
              env:
                  EXPECTED_TAG: "${{ steps.version.outputs.tag }}"
                  GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
              run: |
                  set -euo pipefail

                  if [ -z "$EXPECTED_TAG" ]; then
                    echo "::error::The release version step did not provide an expected tag."
                    exit 1
                  fi

                  tag_ref="refs/tags/${EXPECTED_TAG}"
                  if ! git show-ref --verify --quiet "$tag_ref"; then
                    echo "::error::Expected local tag ${EXPECTED_TAG} does not exist."
                    exit 1
                  fi

                  tag_commit=$(git rev-parse "${tag_ref}^{commit}")
                  head_commit=$(git rev-parse HEAD)
                  if [ "$tag_commit" != "$head_commit" ]; then
                    echo "::error::Tag ${EXPECTED_TAG} points to ${tag_commit}, but HEAD is ${head_commit}."
                    exit 1
                  fi

                  head_tags=$(git tag --points-at HEAD --sort=refname)
                  if [ "$head_tags" != "$EXPECTED_TAG" ]; then
                    displayed_head_tags=${head_tags//$'\n'/, }
                    if [ -z "$displayed_head_tags" ]; then
                      displayed_head_tags="(none)"
                    fi
                    echo "::error::Expected ${EXPECTED_TAG} to be the only tag at HEAD, but found: ${displayed_head_tags}."
                    exit 1
                  fi

                  release_notes_path="${RUNNER_TEMP}/release-notes.md"
                  npm run --silent changelog:release-notes > "$release_notes_path"
                  if [ ! -s "$release_notes_path" ]; then
                    echo "No release notes were generated by git-cliff." > "$release_notes_path"
                  fi
                  echo "path=$release_notes_path" >> "$GITHUB_OUTPUT"

            - name: "Publish to npm"
              if: "steps.npm_check.outputs.already_published != 'true'"
              env:
                  TARBALL_PATH: "${{ steps.release_assets.outputs.tarball_path }}"
              run: 'npm publish "${TARBALL_PATH}" --access public --provenance --ignore-scripts'

            - name: "Create GitHub release"
              uses: "softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b" # v3.0.1
              with:
                  tag_name: "${{ steps.version.outputs.tag }}"
                  name: "${{ steps.version.outputs.tag }}"
                  body_path: "${{ steps.release_notes.outputs.path }}"
                  draft: false
                  files: |
                      ${{ steps.release_assets.outputs.tarball_path }}
                      ${{ steps.release_assets.outputs.zip_path }}
                  fail_on_unmatched_files: true
                  generate_release_notes: false
                  overwrite_files: false
                  prerelease: false
              env:
                  GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"

            - name: "Release summary"
              if: "${{ !cancelled() }}"
              run: |
                  {
                    echo "## 🚀 Release Summary"
                    echo ""
                    echo "- Event: \`${{ github.event_name }}\`"
                    echo "- Actor: @${{ github.actor }}"
                    echo "- Ref: \`${{ steps.target_ref.outputs.ref }}\`"
                    echo "- Source branch: \`${{ steps.target_ref.outputs.branch }}\`"
                    echo "- Package: \`${{ steps.version.outputs.package_name }}\`"
                    echo "- Version: \`${{ steps.version.outputs.version }}\`"
                    echo "- Tag: \`${{ steps.version.outputs.tag }}\`"
                    echo "- Already published on npm: \`${{ steps.npm_check.outputs.already_published }}\`"
                    echo "- npm package: [${{ steps.version.outputs.package_name }}@${{ steps.version.outputs.version }}](${{ steps.version.outputs.npm_release_url }})"
                    echo "- GitHub release: [${{ steps.version.outputs.tag }}](${{ steps.version.outputs.github_release_url }})"
                    echo "- Release assets: \`${{ steps.release_assets.outputs.tarball_name }}\`, \`${{ steps.release_assets.outputs.zip_name }}\`"
                  } >> "$GITHUB_STEP_SUMMARY"
